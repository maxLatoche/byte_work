import pudb

def a_man_must_have_his_nugs():
    non_nugs = [1,2,3,4,5]
    nug_sizes = [20,9,6]
    i = 7
    while i - 6 != non_nugs[-1]:
        pu.db
        for a in range(0,i):
            for b in range(0,i):
                for c in range(0,i):
                    if 6*a+9*b+20*c == i:
                        i += 1
                    else:
                        if c <= i:
                            c += 1
                        elif b <= i:
                            b += 1 

                        # non_nugs.append(i)
                    i += 1
    return non_nugs

print(a_man_must_have_his_nugs())